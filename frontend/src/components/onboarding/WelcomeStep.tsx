import React from 'react';

export const WelcomeStep: React.FC = () => (
  <div>
    <p className="text-[11px] font-semibold uppercase tracking-[0.24em] text-textMuted">Welcome</p>
    <h2 className="mt-2 text-2xl font-bold text-textPrimary">Set up Career OS Pro</h2>
    <p className="mt-3 text-sm leading-6 text-textSecondary">Choose how this device stores data, then finish a lightweight career profile. You can change sync mode later.</p>
  </div>
);
