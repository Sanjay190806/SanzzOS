import React from 'react';

export const ImportBackupStep: React.FC = () => (
  <div className="rounded-xl border border-border-subtle bg-white/[0.04] p-4 text-sm text-textSecondary">
    Import remains available from Settings Backup & Restore. Restores validate the file, save a pre-restore backup, and ask before overwriting local keys.
  </div>
);
