# Recharts Upgrade Blocked

Attempted to upgrade Recharts from v2.12.7 to v3.0.0, but encountered breaking changes in the API (TypeScript errors related to `dataKey` and other props not matching the `Props` overloads for components).

We are keeping Recharts v2 temporarily. 
TODO: Address breaking changes as per the 3.0 migration guide before upgrading.
