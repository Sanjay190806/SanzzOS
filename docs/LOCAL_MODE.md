# Local Mode

Local-only mode remains the default safe fallback for offline use and existing v1.6.x data.

Local mode supports:

- Browser localStorage persistence.
- JSON export/import.
- Backup validation.
- Legacy manual DB snapshots.
- PWA offline shell.

Account cloud sync is optional and requires login.
