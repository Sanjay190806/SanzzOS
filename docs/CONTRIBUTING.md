# Contributing Guidelines

Guidelines for building new features.

## Workflows
1. Create Git branch.
2. Verify TypeScript build compiles cleanly: `npm run build:frontend`.
3. Generate Prisma DB migrations before committing changes.
