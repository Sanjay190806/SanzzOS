# Local To Cloud Migration

The migration wizard is available in Settings.

Options:

- Upload local data to cloud.
- Replace local data with cloud data.
- Keep local-only mode.
- Attempt merge, which currently returns a conflict-required result.

The app saves a pre-migration or pre-restore local backup before overwrite-style actions. No local data is uploaded or replaced silently.
